const React = require('react')
const ReactDOM = require('react-dom')
const ExtensionStoreList = require('./ExtensionStoreList')

ReactDOM.render(
  <ExtensionStoreList />,
  document.getElementById('root')
)
